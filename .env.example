# VTuber Overlay Suite - Environment Variables
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================================
# YouTube Data API Keys (Optional)
# ============================================================
# These keys are embedded into the app at build time.
# If not set, users must provide their own API key (BYOK mode).
#
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable "YouTube Data API v3" in your project.

# Primary API key (used first)
YOUTUBE_API_KEY_PRIMARY=

# Secondary API key (fallback when primary quota is exhausted)
YOUTUBE_API_KEY_SECONDARY=
